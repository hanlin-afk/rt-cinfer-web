# Datasets

- **Workload traces**: synthetic (Poisson/bursty), public cluster traces when licensing allows.
- **Carbon intensity**: regional time series (or synthetic profiles).
- **Hardware params**: node capacities, PUE, DVFS states.

> Provide CSV schemas and generation scripts in `data/` (to be added when code is implemented).
