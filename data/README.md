# Data

`data/samples/synthetic_web_vitals_sample.csv` is a small synthetic sample generated by this repository. It is not real user traffic.

For real deployments, store raw telemetry outside the repository and commit only schemas, aggregated examples, or de-identified samples approved by the data owner.
